import jwt from 'jsonwebtoken';import User from '../models/User.js';import {env} from '../config/env.js';export async function auth(req,res,next){try{const token=req.cookies.accessToken;if(!token)return res.status(401).json({message:'Authentication required'});const p=jwt.verify(token,env.ACCESS_SECRET);req.user=await User.findById(p.sub);if(!req.user)return res.status(401).json({message:'User not found'});next()}catch{res.status(401).json({message:'Session expired'})}}