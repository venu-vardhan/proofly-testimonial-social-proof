import {Router} from 'express';import multer from 'multer';import path from 'node:path';import {fileURLToPath} from 'node:url';import * as c from '../controllers/testimonials.js';import {auth} from '../middleware/auth.js';const r=Router();const dir=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../../uploads');const storage=multer.diskStorage({destination:dir,filename:(req,file,cb)=>cb(null,`${Date.now()}-${Math.round(Math.random()*1e9)}${path.extname(file.originalname)}`)});const upload=multer({storage,limits:{fileSize:3*1024*1024},fileFilter:(req,file,cb)=>cb(null,file.mimetype.startsWith('image/'))});r.post('/public/:spaceSlug',upload.single('avatar'),c.publicCreate);r.get('/owner/:spaceId',auth,c.ownerList);r.patch('/:id/moderate',auth,c.moderate);r.delete('/:id',auth,c.remove);r.get('/wall/:spaceSlug',c.wall);export default r;