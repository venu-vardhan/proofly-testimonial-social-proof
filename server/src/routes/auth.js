import {Router} from 'express';import * as c from '../controllers/auth.js';import {auth} from '../middleware/auth.js';const r=Router();r.post('/signup',c.signup);r.post('/login',c.login);r.post('/refresh',c.refresh);r.post('/logout',c.logout);r.get('/me',auth,c.me);r.get('/verify-email/:token',c.verifyEmail);r.post('/forgot-password',c.forgotPassword);r.post('/reset-password/:token',c.resetPassword);export default r;