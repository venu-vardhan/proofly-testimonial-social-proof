import {useState} from 'react';import {ArrowRight,Eye,EyeOff,LockKeyhole,Sparkles} from 'lucide-react';import {Button,Card} from '../components/ui';import {api} from '../lib/api';import {Link} from '../components/NavLink';
export default function Auth({mode,onAuth}){const signup=mode==='signup';const[show,setShow]=useState(false),[form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setError('');setLoading(true);try{const d=await api(`/auth/${signup?'signup':'login'}`,{method:'POST',body:JSON.stringify(form)});onAuth(d.user);location.href='/dashboard'}catch(e){setError(e.message)}finally{setLoading(false)}}return <div className="auth-page"><div className="auth-side"><Link to="/" className="brand"><span className="brand-mark"><Sparkles/></span><span>Proofly</span></Link><div><p className="eyebrow">SOCIAL PROOF, WITHOUT THE FRICTION</p><h1>Give great customer stories a place to shine.</h1><p>One workspace for collection, moderation, analytics, and publishing.</p></div></div><Card className="auth-card"><p className="eyebrow">{signup?'CREATE YOUR WORKSPACE':'WELCOME BACK'}</p><h2>{signup?'Start collecting proof.':'Pick up where you left off.'}</h2>{error&&<div className="error-box">{error}</div>}<form onSubmit={submit}>{signup&&<label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label>}<label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<div className="password-wrap"><input type={show?'text':'password'} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} minLength="8" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><Button type="submit" size="lg" className="full" disabled={loading}>{loading?'Working…':signup?'Create account':'Sign in'} <ArrowRight/></Button></form>{!signup&&<p className="auth-switch"><Link to="/forgot-password">Forgot password?</Link></p>}<p className="auth-switch">{signup?'Already have an account?':'Need an account?'} <Link to={signup?'/login':'/signup'}>{signup?'Sign in':'Create one'}</Link></p><div className="security-note"><LockKeyhole/> Secure httpOnly token cookies.</div></Card></div>}