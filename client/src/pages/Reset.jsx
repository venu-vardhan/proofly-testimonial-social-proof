import {useState} from 'react';import {ArrowRight,KeyRound} from 'lucide-react';import {Button,Card,Input} from '../components/ui';import {api} from '../lib/api';import {Link} from '../components/NavLink';
export default function Reset(){const token=new URLSearchParams(location.search).get('token')||location.pathname.split('/').pop();const[password,setPassword]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const d=await api(`/auth/reset-password/${token}`,{method:'POST',body:JSON.stringify({password})});setMessage(d.message)}catch(e){setError(e.message)}}return <div className="simple-auth container"><Card className="auth-card"><div className="auth-icon"><KeyRound/></div><p className="eyebrow">NEW PASSWORD</p><h2>Choose a fresh password.</h2>{message&&<div className="success-box">{message}<div><Link to="/login">Return to sign in</Link></div></div>}{error&&<div className="error-box">{error}</div>}{!message&&<form onSubmit={submit}><label>New password<Input type="password" minLength="8" value={password} onChange={e=>setPassword(e.target.value)} required/></label><Button type="submit" size="lg" className="full">Update password <ArrowRight/></Button></form>}</Card></div>}