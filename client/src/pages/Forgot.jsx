import {useState} from 'react';import {ArrowRight,KeyRound,Sparkles} from 'lucide-react';import {Button,Card,Input} from '../components/ui';import {api} from '../lib/api';import {Link} from '../components/NavLink';
export default function Forgot(){const[email,setEmail]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const d=await api('/auth/forgot-password',{method:'POST',body:JSON.stringify({email})});setMessage(d.message)}catch(e){setError(e.message)}}return <div className="simple-auth container"><Card className="auth-card"><div className="brand"><span className="brand-mark"><Sparkles/></span><span>Proofly</span></div><div className="auth-icon"><KeyRound/></div><p className="eyebrow">PASSWORD RECOVERY</p><h2>Reset your password.</h2><p className="muted">Enter your account email. The assessment uses a simulated reset link printed by the server.</p>{message&&<div className="success-box">{message}</div>}{error&&<div className="error-box">{error}</div>}<form onSubmit={submit}><label>Email<Input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><Button type="submit" size="lg" className="full">Send reset link <ArrowRight/></Button></form><p className="auth-switch"><Link to="/login">Back to sign in</Link></p></Card></div>}