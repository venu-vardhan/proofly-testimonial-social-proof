import {useEffect,useState} from 'react';
import Layout from './components/Layout';import Landing from './pages/Landing';import Auth from './pages/Auth';import Dashboard from './pages/Dashboard';import Collect from './pages/Collect';import Wall from './pages/Wall';import Embed from './pages/Embed';import Forgot from './pages/Forgot';import Reset from './pages/Reset';import {api} from './lib/api';
export default function App(){const[user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{api('/auth/me').then(d=>setUser(d.user)).catch(()=>{}).finally(()=>setLoading(false))},[]);if(loading)return <div className="app-loader"><div className="loading-orb"/></div>;const p=location.pathname;let content=p==='/'?<Landing/>:p==='/signup'?<Auth mode="signup" onAuth={setUser}/>:p==='/login'?<Auth mode="login" onAuth={setUser}/>:p==='/forgot-password'?<Forgot/>:p.startsWith('/reset-password/')?<Reset/>:p==='/dashboard'?(user?<Dashboard user={user}/>:<Auth mode="login" onAuth={setUser}/>):p.startsWith('/dashboard/embed')?(user?<Embed/>:<Auth mode="login" onAuth={setUser}/>):p.startsWith('/collect/')?<Collect spaceSlug={p.split('/')[2]}/>:p.startsWith('/wall/')?<Wall spaceSlug={p.split('/')[2]}/>:<Landing/>;async function logout(){await api('/auth/logout',{method:'POST'}).catch(()=>{});setUser(null);location.href='/'}return <Layout user={user} onLogout={logout}>{content}</Layout>}